{
  "name": "@alexpower/rig",
  "version": "3.0.0",
  "description": "Multi-agent build orchestration for Claude Code. File-ownership slices, a plan file as the contract, QA worktrees pinned to HEAD, and checks that must prove they can fail.",
  "type": "module",
  "bin": {
    "rig": "./bin/rig.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "files": [
    "bin",
    "src",
    "harness",
    "templates"
  ],
  "exports": {
    ".": "./harness/index.js",
    "./harness": "./harness/index.js",
    "./*": "./*"
  },
  "scripts": {
    "test": "node test/run.js",
    "demo": "node examples/demo.test.js || true",
    "demo:watch": "node examples/demo-visible.js",
    "dev": "echo 'Rig is a CLI, not a server: run ./bin/rig.js (or `rig` once linked). Start with `rig init`, then `rig up`. See README.md'",
    "check": "biome ci . && npm test && check-no-personal-data --quiet ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13"
  }
}
